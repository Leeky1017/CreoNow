# Rulebook Spec Pointer — creonow-mvp-readiness-remediation

本 Rulebook task 的交付物是 OpenSpec（spec + design + task cards），权威路径如下：

- `openspec/specs/creonow-mvp-readiness-remediation/spec.md`
- `openspec/specs/creonow-mvp-readiness-remediation/design/*.md`
- `openspec/specs/creonow-mvp-readiness-remediation/task_cards/**/*.md`

## Delta Requirements（摘要）

- P0：Dashboard rename/duplicate/archive；Version Preview；Restore confirm；ErrorBoundary；CI 运行 Vitest
- P1：AI history 真数据；核心 services/stores 测试；keytar；XSS；状态冗余；AI retry
- P2：memo/virtualization/useShallow；console 收敛；strings 常量化

细节以 OpenSpec 为 SSOT（本文件只作指针，避免双份规格漂移）。
