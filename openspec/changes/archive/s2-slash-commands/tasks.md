## 1. Specification

- [x] 1.1 审阅并确认 `s2-slash-commands` 仅覆盖 6 个写作命令的注册、过滤、执行路由。
- [x] 1.2 审阅并确认 `specs/editor-delta.md` 场景与路线图命令清单一致。
- [x] 1.3 审阅并确认边界：不重复实现 Slash 框架层触发逻辑。
- [x] 1.4 在进入 Red 前完成 依赖同步检查（Dependency Sync Check）并记录对 C18 的对齐结论。

## 2. TDD Mapping（先测前提）

- [x] 2.1 将 `editor-delta` 的每个 Scenario 映射为可执行测试。
- [x] 2.2 为测试标注 Scenario ID，建立场景到测试的追踪链路。
- [x] 2.3 设定门禁：未出现 Red（失败测试）不得进入实现。

## 3. Red（先写失败测试）

- [x] 3.1 编写“命令注册表包含 6 个目标命令”的失败测试并确认先失败。
- [x] 3.2 编写“关键字过滤命令列表”的失败测试并确认先失败。
- [x] 3.3 编写“选择命令后路由到对应执行器”的失败测试并确认先失败。

## 4. Green（最小实现通过）

- [x] 4.1 以最小实现建立命令接口与注册表使 Red 测试转绿。
- [x] 4.2 仅实现过滤与执行路由必须逻辑，不扩展额外命令语义。

## 5. Refactor（保持绿灯）

- [x] 5.1 收敛重复命令元数据与匹配逻辑，维持单一注册来源。
- [x] 5.2 在不改变行为契约前提下整理命令类型与测试夹具。

## 6. Evidence

- [x] 6.1 在 RUN_LOG 记录 Red/Green 命令与关键输出证据。
- [x] 6.2 记录 Dependency Sync Check 的输入、核对结论与动作。
- [x] 6.3 记录 Main Session Audit 结果并确认签字提交仅变更当前任务 RUN_LOG。
